<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Premium Required - QuizMaster</title>

    <!-- Bootstrap CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <!-- Font Awesome -->
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"
    />
    <!-- Custom CSS -->
    <link rel="stylesheet" href="/styles.css" />
    <style>
      .premium-container {
        max-width: 800px;
        margin: 0 auto;
        padding: 3rem 1rem;
      }
      .premium-card {
        border: none;
        border-radius: 20px;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
        overflow: hidden;
        text-align: center;
        padding: 3rem;
        margin-bottom: 2rem;
      }
      .premium-icon {
        width: 120px;
        height: 120px;
        margin: 0 auto;
        background: linear-gradient(135deg, #f6d365 0%, #fda085 100%);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        margin-bottom: 2rem;
        font-size: 4rem;
        color: white;
      }
      .features-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
        gap: 1.5rem;
        margin: 2rem 0;
      }
      .feature-card {
        background: #f8f9fa;
        border-radius: 15px;
        padding: 1.5rem;
        text-align: center;
        transition: transform 0.3s ease;
      }
      .feature-card:hover {
        transform: translateY(-5px);
      }
      .feature-icon {
        font-size: 2.5rem;
        margin-bottom: 1rem;
        color: var(--primary-color);
      }
      .quiz-list {
        text-align: left;
        max-width: 600px;
        margin: 2rem auto;
      }
      .quiz-list li {
        padding: 0.5rem 0;
        border-bottom: 1px solid #e9ecef;
      }
      .quiz-list li:last-child {
        border-bottom: none;
      }
      .dark-mode .premium-card {
        background-color: #2a2a2a;
      }
      .dark-mode .feature-card {
        background-color: #333;
      }
    </style>
  </head>
  <body>
    <%- include('partials/navbar') %>

    <div class="container premium-container">
      <div class="premium-card">
        <div class="premium-icon">
          <i class="fas fa-lock"></i>
        </div>
        <h1 class="mb-3">Premium Access Required</h1>
        <p class="lead mb-4">You've reached the limit of 3 free quizzes</p>

        <div class="alert alert-info" role="alert">
          <i class="fas fa-info-circle me-2"></i>
          You've already accessed these quizzes:
          <div class="quiz-list">
            <ol>
              <% (locals.accessedQuizzes || []).forEach(function(quizId) { %>
              <li>
                <strong
                  ><%= quizId.replace('custom-', '').replace(/-/g, '
                  ').replace(/_/g, ' ') %></strong
                >
              </li>
              <% }); %>
            </ol>
          </div>
        </div>
        <h3 class="mt-4">Unlock All Quizzes with Premium</h3>
        <p>
          Upgrade to Premium for just <strong>PKR 1,000/month</strong> and get
          unlimited access to all our medical quizzes
        </p>

        <div class="features-grid">
          <div class="feature-card">
            <div class="feature-icon">
              <i class="fas fa-unlock"></i>
            </div>
            <h4>Unlimited Access</h4>
            <p>Access all quizzes without restrictions</p>
          </div>
          <div class="feature-card">
            <div class="feature-icon">
              <i class="fas fa-robot"></i>
            </div>
            <h4>AI Integration</h4>
            <p>Get AI-powered explanations</p>
          </div>
          <div class="feature-card">
            <div class="feature-icon">
              <i class="fas fa-chart-line"></i>
            </div>
            <h4>Detailed Stats</h4>
            <p>Track your progress in depth</p>
          </div>
        </div>

        <a href="/subscription" class="btn btn-primary btn-lg mt-3">
          <i class="fas fa-crown me-2"></i>Upgrade to Premium
        </a>
      </div>

      <div class="text-center">
        <a href="/" class="btn btn-link">
          <i class="fas fa-arrow-left me-2"></i>Return to Homepage
        </a>
      </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
